
public class EntradaDadosWIO {

	/*
	 * public static boolean readBoolean();  // leitura de um boolean
	 * public static boolean readBoolean (String msg); 
	 * public static char readChar(); // leitura de um char
	 * public static char readChar (String msg);
	 * readInt()
	 * readInt(String msg)
	 * readDouble()
	 * readDouble(String msg)
	 * readLine()
	 * readLine(String msg)
	 */
	int i,j;
	double dim;
	boolean flag;
	char car;
	String nome;
	
	i = readInt ("Inteiro: ");
	j = readInt ("Leitura do j: ");
	dim = readDouble ("Double: ");
	flag = readBoolean ("Ok? ");
	car = readChar ("Caracter: ");
	nome = readLine ("Nome: ");
	
}
